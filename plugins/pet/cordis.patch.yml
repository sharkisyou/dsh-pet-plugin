# dsh-pet 单插件 bundle 补丁：安装为 profile bundle 时插入宿主行。
# 该行同时是 dsh.client 扫描的花名册来源（客户端 bundle 经
# window.__DSH_BOOT__ 注入，由 /plugins/dsh-pet/client.js 提供）。
- insert:
    - id: pet
      name: 'dsh-pet'
      inject: [webServer]
